# Loan-Default-Prediction-Logistic-Regression
A machine learning project that predicts loan default risk using Logistic Regression. The project includes data cleaning, EDA, feature engineering, preprocessing, model evaluation, and business recommendations.
